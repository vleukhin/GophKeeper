app:
  name: 'gophkeeper-client'
  version: '1.0.0'

server:
  url: 'http://localhost:8080'

logger:
  log_level: 'debug'
  rollbar_env: 'gophkeeper-client'

storage:
  driver: postgres
  postgres:
    DSN: 'postgres://postgres:postgres@localhost:5454/gophkeeper?sslmode=disable'